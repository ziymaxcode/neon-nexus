import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cybercafe.manager',
  appName: 'Cyber Cafe Manager',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
